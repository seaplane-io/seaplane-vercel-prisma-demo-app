# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# databases replace variables between <> with your credentials both for the local shadow database and the Seaplane SQL database 
DATABASE_URL="postgresql://<SEAPLANE-SQL-USERNAME>:<SEAPLANE-SQL-PASSWORD>@sql.cplane.cloud:5432/<SEAPLANE-SQL-DB>?schema=public"
SHADOW_DATABASE_URL="postgresql://<LOCAL-USERNAME>:<LOCAL-PASSWORD>@localhost:<LOCAL-PORT>/<LOCAL-DB-NAME>?schema=public"

# GitHub application authentication replace variables between <> with your app variables
GITHUB_ID=<YOUR-ID>
GITHUB_SECRET=<YOUR-SECRET>
NEXTAUTH_URL=<YOUR-URL>